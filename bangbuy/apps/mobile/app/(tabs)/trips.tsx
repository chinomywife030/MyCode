/**
 * 行程 Tab 頁面
 * 
 * 注意：此頁面顯示的是「需求」內容（橘色 Hero、WishCard 列表）
 * 這是為了交換「需求」與「行程」兩個 tab 的內容
 */
import { WishesPageContent } from './_wishes-page-content';

export default function TripsScreen() {
  return <WishesPageContent />;
}
