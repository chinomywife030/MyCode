/**
 * 需求 Tab 頁面
 * 
 * 注意：此頁面顯示的是「行程」內容（藍色 Hero、TripCard 列表）
 * 這是為了交換「需求」與「行程」兩個 tab 的內容
 */
import { TripsPageContent } from './_trips-page-content';

export default function WishesScreen() {
  return <TripsPageContent />;
}
