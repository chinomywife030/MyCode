# 🎨 UI 品牌風格重設計

## 📋 改動檔案清單

### 新增檔案

#### A. Theme Tokens
1. **`apps/mobile/src/theme/tokens.ts`**
   - 定義顏色系統（brandOrange、bg、card、text 等）
   - 定義圓角系統（sm/md/lg/xl/full）
   - 定義間距系統（xs/sm/md/lg/xl/2xl/3xl）
   - 定義陰影系統（iOS + Android 通用）
   - 定義字體大小和粗細

#### B. UI 組件（`apps/mobile/src/ui/`）
2. **`apps/mobile/src/ui/Screen.tsx`**
   - 統一背景色、padding、SafeArea 的 Screen 容器

3. **`apps/mobile/src/ui/TopBar.tsx`**
   - 頂部導航欄：左側 Logo、右側通知 + 頭像

4. **`apps/mobile/src/ui/HeroBanner.tsx`**
   - 橘色漸層 Hero Banner，包含標題和白色圓角按鈕
   - 使用 `expo-linear-gradient`

5. **`apps/mobile/src/ui/SearchRow.tsx`**
   - 搜尋框 + 篩選按鈕

6. **`apps/mobile/src/ui/Card.tsx`**
   - 通用卡片組件：白底、圓角 16、柔和陰影

7. **`apps/mobile/src/ui/Tag.tsx`**
   - Tag 組件：國家標籤等

8. **`apps/mobile/src/ui/WishCard.tsx`**
   - Wish 卡片：上半部大圖（或 placeholder）、左上角國家 Tag、下半部 title

### 修改檔案

9. **`apps/mobile/app/(tabs)/index.tsx`**
   - 使用新的 UI 組件：Screen、TopBar、HeroBanner、SearchRow、WishCard
   - 保持功能邏輯不變（Supabase 讀取、導航）
   - 添加搜尋功能（過濾 wishes）

10. **`apps/mobile/app/wish/[id].tsx`**
    - 使用新的 UI 組件：Screen、TopBar、Card
    - 各個區塊使用 Card 組件包裝
    - 保持功能邏輯不變（Supabase 讀取、導航、回覆）

### 依賴套件

11. **`apps/mobile/package.json`**
    - 新增 `expo-linear-gradient`（用於 Hero Banner 漸層）

## 🎨 設計系統

### 顏色系統
- **品牌橘色**：`#f97316` (orange-500) / `#ea580c` (orange-600)
- **背景色**：`#f9fafb` (gray-50) - 很淡的灰
- **卡片背景**：`#ffffff` (白色)
- **文字色**：`#111827` (gray-900) / `#6b7280` (gray-500)

### 圓角系統
- **卡片**：16px (radius.lg)
- **按鈕**：12px (radius.md)
- **小元素**：8px (radius.sm)

### 間距系統
- **卡片內距**：16px (spacing.lg)
- **區塊間距**：16-24px (spacing.lg - spacing['2xl'])

### 陰影系統
- **卡片陰影**：shadow-sm（iOS + Android 通用）

## 📱 UI 結構

### 首頁 (`app/(tabs)/index.tsx`)

```
Screen (統一背景色)
  ├─ TopBar (Logo + 通知 + 頭像)
  ├─ HeroBanner (橘色漸層 + 標題 + 白色按鈕)
  ├─ SearchRow (搜尋框 + 篩選按鈕)
  ├─ Section Header ("熱門需求")
  └─ FlatList
      └─ WishCard (白底卡片、圓角 16、圖片 + Tag + 標題)
```

### 詳情頁 (`app/wish/[id].tsx`)

```
Screen (統一背景色)
  ├─ TopBar
  └─ ScrollView
      ├─ Card (標題 + Tag)
      ├─ Card (描述)
      ├─ Card (連結)
      ├─ Card (其他資訊)
      ├─ Button (我要回覆/報價)
      ├─ Card (最新回覆)
      └─ Link (返回首頁)
```

## ✅ 驗收標準

- [x] 首頁一眼看上去與網站同一設計語言（橘色 banner、搜尋列、熱門需求卡片）
- [x] 所有圓角、陰影、背景色一致（由 tokens 控制）
- [x] 不影響資料讀取與導航
- [x] Hero Banner 使用橘色漸層
- [x] 卡片使用白底、圓角 16、柔和陰影
- [x] 整體頁背景為很淡的灰（不是純白）
- [x] 卡片與區塊有良好留白

## 🔧 技術細節

### 組件化
- 所有 UI 組件都使用 theme tokens
- 組件可復用，樣式統一管理

### 功能邏輯保持不變
- Supabase 讀取邏輯不變
- 導航邏輯不變
- 認證邏輯不變
- 僅改 UI 結構與 styling

### 響應式設計
- 使用 SafeAreaView 處理安全區域
- 使用 Flexbox 布局
- 間距和字體大小使用 tokens 統一管理






