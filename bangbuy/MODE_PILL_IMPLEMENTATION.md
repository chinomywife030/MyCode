# 💊 身分膠囊（Mode Pill）實作報告

## 📅 完成日期
2025-12-16

---

## 🎯 設計目標

將 Header 中的大型「身分切換按鈕」改為小巧的「身分膠囊」：
- **小**：高度 28px，不佔位
- **不佔位**：視覺權重低於 Logo
- **0 學習成本**：icon + 文字，一眼可辨

---

## ✅ 已完成的修改

### 修改的檔案

1. **`bangbuy/components/ModeToggle.tsx`** - 完全重構為膠囊設計
2. **`bangbuy/components/Navbar.tsx`** - 移除 `compact` prop

---

## 📏 具體設計規格

### 一、外觀與尺寸

#### 高度：28px（固定）
```tsx
style={{ height: '28px' }}
```

#### 膠囊樣式
```tsx
className="rounded-full"  // 完全圓角
```

#### 內容：icon + 文字
```tsx
// 買家模式
🛒 買家

// 代購模式
✈️ 代購
```

#### 顏色規則
```tsx
// 買家模式（藍色）
bg-blue-50      // 淺藍背景
text-blue-600   // 藍色文字
hover:bg-blue-100

// 代購模式（橘色）
bg-orange-50    // 淺橘背景
text-orange-600 // 橘色文字
hover:bg-orange-100
```

#### Padding
```tsx
paddingLeft: '10px'
paddingRight: '12px'
```

#### 字級
```tsx
fontSize: '13px'  // 文字
fontSize: '14px'  // icon
```

---

### 二、互動行為

#### 點擊切換身份
```tsx
const handleToggle = () => {
  toggleMode();  // 切換身份
  setToastMessage(/* 提示訊息 */);
  setShowToast(true);  // 顯示 toast
};
```

#### Toast 提示
```tsx
// 買家模式
「已切換為買家模式」

// 代購模式
「已切換為代購模式」
```

#### Toast 樣式
- 位置：`top-20` 置中
- 背景：當前身份主色（藍/橘）
- 文字：白色
- 自動消失：2 秒
- 動畫：`fadeInDown`

---

### 三、視覺權重

```
Logo（最高）> 身分膠囊 > 一般 icon
```

#### 對比

| 元素 | 高度 | 視覺權重 |
|-----|------|---------|
| Logo | 32px | 最高 |
| 身分膠囊 | 28px | 中等 |
| 通知 icon | 24px | 低 |
| 用戶頭像 | 36px | 中高 |

---

## 📊 修改前後對比

### 修改前（大型切換按鈕）

```tsx
// 尺寸
minHeight: 44px
minWidth: 85px

// 樣式
border-2 border-gray-200
兩個按鈕並排

// 視覺
佔據大量空間
視覺權重過高
```

### 修改後（身分膠囊）

```tsx
// 尺寸
height: 28px
width: auto (~70px)

// 樣式
rounded-full
單一膠囊

// 視覺
小巧不佔位
視覺權重適中
```

---

## 🎨 設計細節

### 1. 膠囊形狀
```tsx
rounded-full  // 完全圓角，像藥丸
```

### 2. 顏色使用
```tsx
// 使用淺色背景 + 主色文字
// 不使用實心主色（避免過度搶眼）

買家：bg-blue-50 + text-blue-600
代購：bg-orange-50 + text-orange-600
```

### 3. Hover 效果
```tsx
hover:bg-blue-100   // 背景略深
hover:shadow-md     // 加陰影
active:scale-95     // 點擊縮小
```

### 4. 間距
```tsx
gap-1.5  // icon 與文字間距
```

---

## 💡 互動流程

### 使用者點擊膠囊

```
1. 點擊膠囊
   ↓
2. 身份立即切換（買家 ↔ 代購）
   ↓
3. 膠囊顏色改變（藍 ↔ 橘）
   ↓
4. Toast 提示出現（2 秒後消失）
   ↓
5. Hero / CTA 顏色跟隨切換
```

---

## 🔍 驗收標準

### 視覺檢查
- [x] 膠囊高度 28px（不超過）
- [x] 使用 rounded-full（完全圓角）
- [x] 只顯示 icon + 文字
- [x] 買家模式：🛒 買家（藍色）
- [x] 代購模式：✈️ 代購（橘色）
- [x] 視覺權重低於 Logo

### 互動檢查
- [x] 點擊即切換身份
- [x] 顯示 toast 提示
- [x] Toast 2 秒後自動消失
- [x] Hero 顏色跟隨切換

### Header 檢查
- [x] Header 高度未增加
- [x] 手機視窗下不擁擠
- [x] 一眼可知當前身份

---

## 📱 響應式設計

### 桌面版
```
Logo | 身分膠囊 | ... | 通知 | 用戶
```

### 手機版
```
☰ Logo 身分膠囊 | 通知 用戶
```

**膠囊在所有尺寸下保持 28px 高度**

---

## 🎯 設計優勢

### 1. 小巧不佔位
- 高度僅 28px
- 寬度自適應（~70px）
- 不影響 Header 高度

### 2. 0 學習成本
- icon 直觀（🛒 買家 / ✈️ 代購）
- 顏色清晰（藍 / 橘）
- 文字明確

### 3. 一眼可辨
- 當前身份一目了然
- 顏色與 Hero 一致
- 視覺連貫性強

### 4. 互動流暢
- 點擊即切換
- Toast 即時反饋
- 動畫流暢

---

## 🔧 技術實作

### Toast 動畫
```tsx
@keyframes fadeInDown {
  from {
    opacity: 0;
    transform: translate(-50%, -10px);
  }
  to {
    opacity: 1;
    transform: translate(-50%, 0);
  }
}
```

### 自動消失
```tsx
useEffect(() => {
  if (showToast) {
    const timer = setTimeout(() => {
      setShowToast(false);
    }, 2000);
    return () => clearTimeout(timer);
  }
}, [showToast]);
```

### 狀態管理
```tsx
const [showToast, setShowToast] = useState(false);
const [toastMessage, setToastMessage] = useState('');
```

---

## 📝 使用方式

### 在 Navbar 中使用
```tsx
import ModeToggle from '@/components/ModeToggle';

<ModeToggle className="shrink-0" />
```

### Props
```tsx
interface ModeToggleProps {
  className?: string;  // 自定義 className
}
```

---

## ✅ 無障礙支援

```tsx
aria-label={`當前身份：${mode === 'requester' ? '買家' : '代購'}，點擊切換`}
```

---

## 🎉 完成效果

### 修改前
- ❌ 切換按鈕太大（44px 高）
- ❌ 佔據過多空間
- ❌ 視覺權重過高
- ❌ 兩個按鈕並排

### 修改後
- ✅ 膠囊小巧（28px 高）
- ✅ 不佔位
- ✅ 視覺權重適中
- ✅ 單一膠囊，清晰明確
- ✅ 點擊切換 + Toast 反饋
- ✅ 0 學習成本

---

## 🚀 驗證方式

### 1. 視覺檢查
```
1. 打開首頁
2. 觀察 Header
   ✅ 膠囊高度 28px
   ✅ 顏色清晰（藍/橘）
   ✅ 不佔太多空間
```

### 2. 互動測試
```
1. 點擊膠囊
   ✅ 身份立即切換
   ✅ Toast 提示出現
   ✅ Hero 顏色改變
2. 等待 2 秒
   ✅ Toast 自動消失
```

### 3. 響應式測試
```
1. 手機模擬（Chrome DevTools）
   ✅ 膠囊不會太大
   ✅ Header 不擁擠
   ✅ 可輕鬆點擊
```

---

**完成者**：AI Assistant  
**完成日期**：2025-12-16  
**版本**：v5.0 (Mode Pill)










